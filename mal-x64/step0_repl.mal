(load-file "funcs.mal")
(load-file "nasm.mal")

(def! _start
  (list
    [:section '.text]
    [:global '_start]

    '_start
    [:mov :rdi 44]
    [:mov :rax 60]
    [:syscall]))


(print-asm! _start)
